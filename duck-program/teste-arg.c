#include <stdio.h>

// argc = argument count | conta os argumentos passados
// argv[] = argument vector | array de argumentos recebidos
int main(int argc, char *argv[]){
	printf("Parametro: %s\n", argv[1]);
	return 0;
}
